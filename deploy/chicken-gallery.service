# systemd unit for the gallery. Install to /etc/systemd/system/chicken-gallery.service
# then:  sudo systemctl daemon-reload && sudo systemctl enable --now chicken-gallery
[Unit]
Description=Chicken Butt Gallery
After=network.target

[Service]
Type=simple
User=gallery
Group=gallery
WorkingDirectory=/opt/chicken-gallery
EnvironmentFile=/opt/chicken-gallery/.env
ExecStart=/usr/bin/node server.js
Restart=on-failure
RestartSec=3

# --- sandboxing: the process may only write to its data + assets dirs ---
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ProtectKernelTunables=true
ProtectControlGroups=true
ReadWritePaths=/opt/chicken-gallery/data /opt/chicken-gallery/assets /opt/chicken-gallery/backups

[Install]
WantedBy=multi-user.target
